# CLAUDE.md

このリポジトリで作業するときの約束事。**セッションをまたいで引き継ぐべき文脈**をここに置く。

## このプロジェクトは何か

オープンソースのロボットアーム **SO-101** と **LeRobot** で、
最終的に**ハンドドリップでコーヒーを淹れる（100〜200ml）**ところまで到達することを目指す記録。

到達できるかは未確定。**何を買い、何につまずき、どこまでできたかを残すこと自体が目的**でもある。

全体像は [README.md](README.md)、最終目標と現時点の壁は [docs/99-coffee.md](docs/99-coffee.md) を見る。

---

## 作業の再開手順

```bash
# 1. 今どのフェーズにいるかを見る
gh issue list --milestone "Phase 0: 実機なしで学習を回す" --state open

# 2. 実機がまだ届いていない場合、今できるものだけ抽出する
gh issue list --state open --label "blocked-by-hardware" --state open   # 待ちのもの
gh issue list --state open --search "-label:blocked-by-hardware"        # 今できるもの
```

Milestone は README のフェーズ表と 1:1 で対応している。

issue は**固定ではない**。進むにつれて増減させる。
実際にやってみて不要と分かったものは閉じ、必要が判明したものは追加する。

---

## 書いてはいけないこと

このリポジトリは **public**。以下は明確に禁止する。

### 1. クラウド事業者名・調達元

学習は借用した GPU インスタンスで回すが、**どこから借りているかは一切書かない。**

書いてよい: GPU 型番、VRAM、RAM、CUDA バージョン、所要時間、ステップ数
書かない: 事業者名、サービス名、プラン名、料金

うっかり漏れやすいもの:

- `/workspace` のような**プロバイダ特有のパス**
- pod ID / インスタンス ID / ホスト名
- ホスト名を含む `nvidia-smi` や `hostname` の出力（**GPU 型番だけを転記する**）
- 管理コンソールのスクリーンショット
- 事業者固有の CLI コマンド

#### 公開先は GitHub だけではない

**この repo をいくら grep してもクリーンなのに、別の経路から漏れることがある。**
2026-07-28 に実際にやらかした（[経緯](docs/02-before-arm.md#やらかし-学習設定からプロバイダのパスが公開された)）。

公開前に必ず全部を確認する。

| 公開先 | 何が漏れるか |
|---|---|
| **Hugging Face model repo** | `train_config.json` の `output_dir` にパスがそのまま入る。**デフォルトで public 作成される** |
| **Hugging Face dataset repo** | カメラ画像の背景。収録場所が丸ごと写る |
| **W&B** | ホスト名・GPU 情報・環境変数を自動収集する。プロジェクトは private にする |
| GitHub | commit 前に grep |

対策:

- **`--output_dir` は `$HOME` 配下などプロバイダ非依存のパスにする。**
  永続ボリュームを使いたい場合も、パスを直接書かず `PERSIST_DIR` 経由にする
- `--policy.repo_id` で作られる Hub repo は**作成直後に private 化する**か、
  最初から `--policy.private=true` 相当の設定を確認してから回す
- 公開するのは最終成果物だけにし、その際に設定を洗浄する

`setup.sh` は**素の Ubuntu コンテナ前提**で書く。プロバイダ固有の要素を混ぜない。
永続ボリュームのパスは `PERSIST_DIR` 環境変数で外から渡す。

**例外**: 公式ドキュメントが選択肢として挙げているものを**引用する**のは可。
（例: LeRobot 公式が GPU なしの代替として案内している Colab ノートブックへのリンク）
自分がどこで回したかを書かなければよい。

### 2. 有料コンテンツの転記

[oggata「SO-101ロボットアーム 実践ガイド」（Zenn Books）](https://zenn.dev/oggata/books/41ce1497ddde6a) は**有料本**。
本文を転記しない。参照が必要なときは**章番号だけ**を書く。

### 3. 認証情報

HF トークン、W&B の API キー、その他一切。
環境変数で渡す運用にし、リポジトリにもコンテナイメージにも焼かない。
`.gitignore` で `.env` / `*.token` / `*.key` を除外済み。

### 4. データセットの背景

カメラ画像データセットを Hugging Face Hub に public で push すると、
**背景（部屋・書類・モニタの中身）がそのまま公開される。**
無地の背景紙を敷いた固定セットで撮影する。

---

## 役割分担

| 場所 | 担当 |
|---|---|
| **手元の Mac（Apple M2 Pro / 32GB / macOS 14.6.1 / arm64）** | gym-hil テレオペ、データセット記録・可視化、実機接続 |
| **借用 GPU インスタンス** | 学習のみ（SmolVLA / ACT）。**毎回リセットされる前提** |
| **Hugging Face Hub** | データセットとチェックポイントの受け渡し |
| **W&B** | 学習ログ（インスタンスが消えても残る） |
| **ZBOX（アーム到着後に導入予定）** | アームの隣に置く推論機 |

**gym-hil のテレオペはクラウドに置けない。** MuJoCo の描画とキーボード/ゲームパッド入力が必要で、
ヘッドレスの GPU インスタンスとは相性が悪い。macOS では `mjpython` を使う。

借用 GPU が毎回リセットされる問題は、[`setup.sh`](setup.sh) と
「成果物は Hub に置く」運用で吸収する。LeRobot はもともと計算機が使い捨てである前提の設計になっている。

---

## ドキュメントの運用ルール

| ファイル | ルール |
|---|---|
| [docs/90-references.md](docs/90-references.md) | **実際に読んだものだけ**を載せる。未読は末尾の「積読」に置き、読んだら上に移す |
| [docs/99-coffee.md](docs/99-coffee.md) | 見立てが実測で覆ったら**書き換える**。更新履歴の表に日付付きで残す |
| [docs/01-hardware.md](docs/01-hardware.md) | 価格は入手日時点のもの。変わったら日付を添えて更新 |
| 実行環境の記録テーブル | **スペックと再現手順は詳しく、調達元は書かない** |
| 各 docs の「詰まったこと」欄 | うまくいった話より価値がある。**必ず埋める** |

日付は相対表現（「先週」等）を使わず、**絶対日付**で書く。

---

## 技術メモ

- LeRobot は **Python 3.12 以上 / PyTorch 2.10 以上**が必要
- **借用インスタンスのリセット対策**: `--save_checkpoint_to_hub=true`（デフォルト false）で
  チェックポイントを Hub に送り、`--resume=true --config_path=<Hub repo id>` で別マシンから再開できる。
  `--save_checkpoint_to_hub` には `--policy.repo_id` の指定が必須 →
  [docs/02-before-arm.md](docs/02-before-arm.md#リセットを越えて学習を続ける)
- SO-101 のサーボ制御には `feetech` extra が必須
- **リーダーアームは関節ごとにギア比が違う**（1/191, 1/345, 1/147）。フォロワーは全部 1/345。
  組立時に取り違えると動かない → [docs/03-assembly.md](docs/03-assembly.md)
- SO-101 の可搬は **400g**。これが [ハンドドリップの最大の壁](docs/99-coffee.md)
- データ量は **50 エピソードが下限**。公式が「25 では足りなかった」と明記している
- USB カメラは `fourcc: "MJPG"` を指定しないと帯域不足で fps が落ちることがある
- **公式 SmolVLA のコマンドはそのままでは動かない**（v0.6.1 で確認）。
  `--policy.repo_id` か `--policy.push_to_hub=false` が要り、さらにカメラ名が合わないので
  `--rename_map` が要る → [docs/02-before-arm.md](docs/02-before-arm.md#公式ドキュメントのコマンドはそのままでは-2-回落ちる)
- **自分でデータを録るときはカメラ名を `camera1` / `camera2` にする。** 後で `--rename_map` を書かずに済む
- 手元の Mac（MPS）は **約 11〜15 秒/step**（batch=2）。パイプラインの疎通確認には十分だが、
  本番の学習は借用 GPU が必須。A100-80GB は **1.42 step/s**（batch=64）で約 20 倍
- **Linux では `ffmpeg=7.1.1` 固定と `LD_LIBRARY_PATH=$CONDA_PREFIX/lib` が必須。**
  無いと torchcodec がロードできず、学習がデータ読み込み直前で死ぬ。
  `setup.sh` に対処済みだが、環境を手で作るときは忘れやすい →
  [docs/02-before-arm.md](docs/02-before-arm.md#linux-でだけ落ちるバグを-2-つ潰した)
- **`save_freq` のデフォルトは 20,000。** `--steps=20000` だと最後に 1 回しか保存されず、
  途中で落ちると全損する。長時間の学習では `--save_freq` を明示する
- **実験管理は W&B 一択。** LeRobot に TensorBoard / MLflow の入口は無く、`WandBConfig` のみ。
  `WANDB_API_KEY` を環境変数で渡す（`setup.sh` が対応済み）。
  なお W&B はホスト名や環境情報を自動収集するため、**プロジェクトは private にし、
  repo には数値だけ転記する**（調達元が読めてしまうため）

---

## コミットについて

- コミットメッセージは英語、本文で「何を・なぜ」を説明する
- ドキュメントのみの変更でも、判断の経緯が分かるように書く
